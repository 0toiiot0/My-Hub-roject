# MY HUB — DESIGN.md

> 디자인 시스템 및 구현 규칙
> 작성일: 2026-08-13 / 상태: 초안

이 문서는 `PRD.md`의 기능 요구사항에 대응하는 시각 규칙을 정의한다. 구현 시 이 문서의 값을 그대로 사용하고, 여기 없는 값은 새로 만들지 말고 문서를 먼저 갱신한다.

---

## 1. 무드

### 1.1 한 줄 정의
**잘 짜인 책의 속표지.** 종이색 배경, 장식 없는 조판, 넉넉한 여백.

### 1.2 성격
미니멀과 문서(아카이브) 톤의 결합이다. 두 톤 모두 "장식을 빼고 내용을 읽게 만든다"는 같은 목표를 가지므로 충돌하지 않는다.

| 미니멀에서 가져온 것 | 문서에서 가져온 것 |
| --- | --- |
| 장식 요소 없음 | 종이 같은 오프화이트 배경 |
| 넉넉한 여백 | 인쇄물처럼 정돈된 조판 |
| 최소한의 색 | 로고의 세리프 서체 |
| 선과 테두리 최소화 | 위계가 분명한 타입 스케일 |

### 1.3 판단 기준
새로운 요소를 넣을지 고민될 때 적용하는 질문:

1. 이걸 빼도 내용이 전달되는가? → 전달되면 뺀다.
2. 인쇄된 책에 이런 게 있는가? → 없으면 다시 생각한다.
3. 이게 내용보다 먼저 눈에 들어오는가? → 그렇다면 약하게 만든다.

---

## 2. 컬러

### 2.1 팔레트

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--bg` | `#FAF9F6` | 페이지 배경 (종이색) |
| `--bg-hover` | `#F2F0EB` | 카드 hover 배경 |
| `--text` | `#1A1A1A` | 제목, 본문 (먹색) |
| `--text-muted` | `#6B6863` | 부가 설명, 한 줄 소개, 푸터 |
| `--border` | `#E5E3DE` | 카드 테두리, 구분선 |
| `--accent` | `#4A6D80` | 링크, 상태 배지, 포커스 링 |
| `--accent-strong` | `#3A5665` | accent의 hover / active |
| `--accent-tint` | `#EAEFF2` | 배지 배경 |

### 2.2 포인트 컬러 사용 규칙

`--accent`는 **아래 세 곳에만** 사용한다.

1. 링크 (텍스트 링크, 푸터의 연락처)
2. 상태 배지 (`진행중`, `완료`)
3. 포커스 링, 카드 hover 시 테두리

**절대 사용하지 않는 곳**: 제목, 본문, 배경 넓은 면적, 아이콘 채우기, 구분선.

> 색이 하나 생기면 여기저기 칠하고 싶어지지만, 그 순간 종이 톤이 무너진다. 이 규칙이 컬러 섹션에서 가장 중요하다.

### 2.3 금지 색상
- `#FFFFFF` (순백색) — 배경은 항상 `--bg`
- `#000000` (순검정) — 텍스트는 항상 `--text`
- 위 팔레트에 없는 색 전부

### 2.4 대비 확인
`--accent` (`#4A6D80`) 대 `--bg` (`#FAF9F6`) = **5.26:1** → WCAG AA 통과 (본문 텍스트 기준 4.5:1 이상)

---

## 3. 타이포그래피

### 3.1 서체 3종

| 역할 | 서체 | 사용처 |
| --- | --- | --- |
| 로고 | 영문 세리프 (EB Garamond) | `MY HUB` 로고 **전용** |
| 콘텐츠 | 산세리프 (Noto Sans KR) | 읽는 텍스트 — 카드 제목·설명, 모달 제목·본문, 인트로 한 줄 소개 |
| UI / 라벨 | 모노스페이스 (IBM Plex Mono) | 읽지 않고 식별하는 텍스트 — 순번, 배지, 섹션 제목, 스택 태그, 헤더, 푸터, ENTER |

```css
--font-logo: 'EB Garamond', Georgia, serif;
--font-body: 'Noto Sans KR', -apple-system, BlinkMacSystemFont, sans-serif;
--font-mono: 'IBM Plex Mono', 'SFMono-Regular', Consolas, monospace;
```

**분배 원칙**: 사용자가 **읽는** 텍스트는 산세리프, **식별하는** 라벨은 모노스페이스.

문단으로 읽히는 내용(문제 정의, 해결 방식, 주요 판단 등)은 산세리프여야 피로하지 않다. 반면 순번·배지·섹션 제목처럼 짧고 기능적인 텍스트는 모노스페이스로 두면 개발 프로젝트라는 신호가 생기면서 종이 톤은 유지된다. 이 경계를 넘지 않는 것이 핵심이다 — 모달 본문까지 모노스페이스로 가면 읽기 어려워지고 톤이 터미널 쪽으로 무너진다.

### 3.2 로고 전용 서체의 근거
로고가 유일한 세리프이므로 자동으로 특별해진다. 인트로 화면에서 세리프는 로고뿐이고, 목록 화면은 전부 산세리프다. 로고가 표지 역할을 하고 내용은 깔끔하게 읽히는 구조다.

또한 `MY HUB`는 영문 두 단어이므로 **영문 세리프만 로드하면 된다.** 한글 세리프(본명조)는 글자 수 때문에 웹폰트 용량이 크지만, 이 경우 해당하지 않는다.

**규칙**: 로고 서체를 로고 외의 어떤 요소에도 쓰지 않는다.

### 3.3 타입 스케일

| 요소 | 서체 | 데스크톱 | 모바일 | 굵기 | 자간 | 행간 |
| --- | --- | --- | --- | --- | --- | --- |
| 로고 | logo | 72px | 40px | 400 | `0.2em` | 1.1 |
| 인트로 한 줄 소개 | body | 15px | 14px | 400 | `0` | 1.6 |
| ENTER 버튼 | **mono** | 13px | 13px | 500 | `0.2em` | 1 |
| 헤더 이름 | **mono** | 14px | 14px | 400 | `0.06em` | 1 |
| 카드 제목 | body | 18px | 17px | 500 | `0` | 1.4 |
| 카드 한 줄 설명 | body | 14px | 14px | 400 | `0` | 1.6 |
| 카드 순번 | **mono** | 12px | 12px | 400 | `0.1em` | 1 |
| 상태 배지 | **mono** | 11px | 11px | 400 | `0.1em` | 1 |
| 모달 제목 | body | 28px | 24px | 500 | `-0.01em` | 1.3 |
| 모달 섹션 제목 | **mono** | 12px | 12px | 400 | `0.14em` | 1 |
| 모달 본문 | body | 16px | 15px | 400 | `0` | **1.7** |
| 기술 스택 태그 | **mono** | 12px | 12px | 400 | `0.02em` | 1 |
| 푸터 | **mono** | 12px | 12px | 400 | `0.06em` | 1.7 |

> 모노스페이스로 옮긴 요소는 굵기를 `400`으로 내리고 자간을 늘렸다. 모노스페이스는 산세리프보다 시각적으로 무거워서, 같은 굵기·자간을 유지하면 라벨이 본문보다 강해 보인다.

### 3.4 대문자 처리
아래 요소는 대문자 + 넓은 자간으로 조판한다. (`text-transform: uppercase`)

- 로고 `MY HUB`
- `ENTER` 버튼
- 모달 섹션 제목 (`PROBLEM`, `SOLUTION` 등 — 또는 한글이면 대문자 처리 없이 자간만)
- 상태 배지

### 3.5 굵기 제한
`400`(regular)과 `500`(medium) **두 가지만** 사용한다. `700`(bold) 이상은 이 톤에서 과하게 무겁다. 강조가 필요하면 굵기 대신 크기, 색(`--text-muted` → `--text`), 여백으로 해결한다.

**모노스페이스는 `400`만 사용한다.** 모노스페이스의 `500`은 글자가 뭉개지면서 라벨이 필요 이상으로 두드러진다. 예외는 `ENTER` 버튼 하나 — 화면의 유일한 행동 유도 요소이므로 `500`을 허용한다.

### 3.6 모노스페이스와 한글
`IBM Plex Mono`에는 한글 글리프가 없다. 모노스페이스로 지정한 요소 중 한글이 들어가는 것(`진행중`, `완료`, 헤더 이름, 한글 섹션 제목)은 자동으로 `--font-body`로 대체된다.

영문·숫자는 모노스페이스로, 한글은 산세리프로 렌더되며 크기는 통일돼 있어 어색하지 않다. 다만 아래를 지킨다:

- 모노스페이스 지정 요소의 `font-family`는 항상 `var(--font-mono), var(--font-body)` 순서로 fallback을 명시한다
- 한글과 영문이 **한 줄에 섞이는** 라벨은 만들지 않는다 (예: `진행중 (in progress)`) — 두 서체가 나란히 놓이면 자폭 차이가 눈에 띈다
- 상태 배지는 한글(`진행중`, `완료`) 또는 영문(`IN PROGRESS`, `DONE`) 중 하나로 통일한다

**자간은 통일되지 않는다 — 한글 전용 라벨은 `letter-spacing: 0`으로 재정의한다.** 한글 완성형 글자는 이미 정사각형에 가까운 형태라 로마자 대문자 트래킹용으로 잡은 `0.06em`~`0.2em` 자간을 그대로 적용하면 글자 사이가 헐겁게 벌어져 보인다(`수 정`, `취 소`, `로 그 인`처럼). 모노스페이스로 지정한 요소 중 **내용이 항상 한글로만 렌더되는 것**(`수정` 버튼, 로그인/편집 폼의 `로그인`·`취소`·`저장` 버튼, `비밀번호`처럼 순한글인 폼 라벨, 헤더 이름)은 자간을 `0`으로 둔다. 영문으로 렌더되는 라벨(`EDIT`, `LOGOUT`, `PROBLEM`, `SOLUTION` 등)은 §3.3의 자간 값을 그대로 유지한다.

---

## 4. 레이아웃

### 4.1 간격 체계
`4px` 배수만 사용한다. 사용 가능한 값:

```
4  8  12  16  24  32  48  64  96  128
```

이 목록에 없는 값(예: `20px`, `35px`)은 쓰지 않는다.

### 4.2 컨테이너
- 최대 폭: `1120px`
- 좌우 여백: 데스크톱 `48px` / 모바일 `24px`

### 4.3 카드 그리드

| 화면 | 열 | 간격 |
| --- | --- | --- |
| 데스크톱 (`≥1024px`) | 4열 × 2행 | `24px` |
| 태블릿 (`640–1023px`) | 2열 × 4행 | `20px` → `24px`로 통일 |
| 모바일 (`<640px`) | 1열 × 8행 | `16px` |

### 4.4 카드
- 비율: 썸네일 영역 `4:3`, 그 아래 텍스트 영역 가변
- 테두리: `1px solid var(--border)`
- 모서리: `2px`
- 내부 여백: `16px`
- 배경: `var(--bg)` (배경과 동일 — 테두리로만 구분)

### 4.5 인트로
- 높이: `100dvh`, 스크롤 없음
- 중앙 정렬 (수직·수평)
- 요소 간격: 로고 → 한 줄 소개 `24px`, 한 줄 소개 → ENTER `64px`
- 인트로에는 헤더와 푸터를 렌더하지 않는다

### 4.6 모달
- 최대 폭: `680px`
- 최대 높이: `85dvh`, 초과 시 내부 스크롤
- 내부 여백: 데스크톱 `48px` / 모바일 `24px`
- 오버레이: `rgba(26, 26, 26, 0.4)`
- 모서리: `2px`
- 그림자: `0 8px 32px rgba(26, 26, 26, 0.12)` — **이 프로젝트에서 그림자를 쓰는 유일한 곳**

---

## 5. 인터랙션

### 5.1 전체 강도
**절제된 수준.** 정적이지만 죽어 있지는 않은 정도. 요소가 스크롤에 따라 나타나거나, 눈에 띄게 움직이는 연출은 넣지 않는다.

### 5.2 타이밍

```css
--ease: cubic-bezier(0.2, 0, 0.2, 1);
--duration-fast: 150ms;   /* hover, 색 변화 */
--duration-base: 220ms;   /* 모달 등장 */
--duration-slow: 400ms;   /* 인트로 fade-in */
```

### 5.3 인트로 진입
- 로고 → 한 줄 소개 → ENTER 순서로 fade-in
- 각 요소 `400ms`, 딜레이 `0ms` / `120ms` / `240ms`
- `opacity` + `translateY(8px)` → `0`

### 5.4 ENTER 클릭
**전환 연출 없이 즉시 `/projects`로 이동한다.**

페이드나 문 열리는 연출을 넣지 않는다. 문서 톤에서는 전환이 없는 게 더 깔끔하고, 재방문 시 매번 기다리게 하는 비용이 없다.

### 5.5 카드 hover

`coming soon` 상태가 아닌 카드에만 적용한다.

| 속성 | 기본 | hover |
| --- | --- | --- |
| 배경 | `var(--bg)` | `var(--bg-hover)` |
| 테두리 | `var(--border)` | `var(--accent)` |
| 위치 | `translateY(0)` | `translateY(-2px)` |

전환: `150ms var(--ease)`

**중요**: 화면 전체 배경이나 다른 카드는 건드리지 않는다. hover한 카드 **자신의** 배경만 변한다.

> 스포트라이트 효과(전체 배경을 어둡게 하고 해당 카드만 밝게)는 채택하지 않았다. 8개 카드가 촘촘한 그리드에서 마우스를 옮길 때마다 화면 전체가 밝아지고 어두워지면 눈이 피로해지고, 종이 톤도 무너진다.

### 5.6 `coming soon` 카드
- hover 시 **아무 변화 없음**
- `cursor: default` (포인터로 바뀌지 않음)
- 전체 불투명도 `0.55`
- 클릭 불가

### 5.7 모달 등장
- 오버레이: `opacity 0 → 1`, `220ms`
- 모달 본체: `opacity 0 → 1` + `translateY(16px) → 0`, `220ms var(--ease)`
- 닫힐 때: 같은 값의 역방향, `180ms`

### 5.8 포커스
```css
:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
```
마우스 클릭 시에는 나타나지 않고 키보드 조작 시에만 나타난다. `outline: none`으로 지우지 않는다.

### 5.9 모션 감소 설정 대응
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```
움직임에 민감한 사용자를 위해 필수로 넣는다.

---

## 6. 컴포넌트 명세

### 6.1 ENTER 버튼
- 서체: `--font-mono`, 13px, `500`, 자간 `0.2em`
- 배경 없음, 테두리 `1px solid var(--text)`
- 내부 여백: `14px 40px`
- 모서리: `2px`
- 텍스트: `--text`
- hover: 배경 `--text`, 텍스트 `--bg` (반전)
- 전환: `150ms`

### 6.2 상태 배지

| 상태 | 텍스트 | 배경 | 글자색 | 테두리 |
| --- | --- | --- | --- | --- |
| `coming-soon` | `COMING SOON` | 없음 | `--text-muted` | `1px solid var(--border)` |
| `in-progress` | `IN PROGRESS` | `--accent-tint` | `--accent-strong` | 없음 |
| `done` | `DONE` | `--accent` | `--bg` | 없음 |

- 서체: `--font-mono`, 11px, `400`, 자간 `0.1em`, 대문자
- 내부 여백: `4px 8px`
- 모서리: `2px`

> 배지 텍스트를 영문으로 통일했다. §3.6에 따라 세 배지 중 하나만 한글이면 서체가 갈려서 자폭이 어긋난다. `COMING SOON`이 이미 영문이므로 나머지를 맞췄다.

### 6.3 기술 스택 태그
- 서체: `--font-mono`, 12px
- 배경 없음, 테두리 `1px solid var(--border)`
- 내부 여백: `4px 8px`
- 모서리: `2px`
- 색: `--text-muted`
- 태그 간 간격: `8px`

### 6.4 카드 썸네일 fallback
이미지가 없을 때:
- 배경: `--bg-hover`
- 중앙에 카드 순번을 `--font-mono` 32px, 색 `--border`로 표시
- 제목을 겹쳐 쓰지 않는다 (텍스트 영역에 이미 있음)

### 6.5 링크
- 색: `--accent`
- 밑줄: 기본 있음, `text-underline-offset: 3px`
- hover: 색 `--accent-strong`
- 외부 링크는 새 탭 (`target="_blank" rel="noopener"`)

### 6.6 헤더
- 서체: `--font-mono`, 14px, `400`, 자간 `0.06em`
- 높이: `64px`, 좌측 정렬
- 하단 테두리: `1px solid var(--border)`
- 이름 클릭 시 `/` 이동, hover 시 색 `--text-muted`

### 6.7 푸터
- 서체: `--font-mono`, 12px, `400`, 자간 `0.06em`
- 상단 테두리: `1px solid var(--border)`
- 상하 여백: `32px`
- 연락처 항목 간 간격: `16px`
- 이메일은 `mailto:`, 그 외는 새 탭

### 6.8 모달 섹션 제목
- 서체: `--font-mono`, 12px, `400`, 자간 `0.14em`, 대문자
- 색: `--text-muted`
- 아래 본문과의 간격: `8px`
- 섹션 간 간격: `32px`
- 영문 통일 권장: `PROBLEM` / `SOLUTION` / `STACK` / `DECISIONS` / `OUTCOME` / `LINKS`

---

## 7. 금지 사항

구현 중 손이 가지만 이 톤을 무너뜨리는 것들이다. 예외를 만들지 않는다.

### 7.1 시각 요소
- [ ] **그라디언트 금지** — 배경, 버튼, 텍스트 전부
- [ ] **그림자 금지** — 예외는 모달 하나뿐 (§4.6)
- [ ] **둥근 모서리 최소화** — `2px` 기본, 어떤 경우도 `4px` 초과 금지
- [ ] **순백색·순검정 금지** — `#FFFFFF`, `#000000`
- [ ] **이모지 금지** — UI, 텍스트 콘텐츠 전부
- [ ] **아이콘 최소화** — 외부 링크 표시 정도만 허용
- [ ] **일러스트, 패턴, 텍스처 금지**

### 7.2 타이포그래피
- [ ] **이탤릭 금지** — 강조는 굵기(`500`) 또는 색으로만 처리
- [ ] **서체 추가 금지** — §3.1의 3종 외 금지
- [ ] **로고 서체 전용** — 로고 외 요소에 세리프 사용 금지
- [ ] **`700` 이상 굵기 금지**
- [ ] **`4px` 배수 아닌 간격 금지**

### 7.3 컬러
- [ ] **포인트 컬러 확장 금지** — 링크·배지·포커스 링 외 사용 금지
- [ ] **팔레트 외 색 금지** — §2.1에 없는 색 추가 금지
- [ ] **제목·본문에 포인트 컬러 금지**

### 7.4 인터랙션
- [ ] **스크롤 기반 등장 애니메이션 금지**
- [ ] **페이지 전환 애니메이션 금지**
- [ ] **자동 재생 요소 금지** — 캐러셀, 슬라이드쇼, 반복 애니메이션
- [ ] **화면 전체를 변화시키는 hover 금지** — 스포트라이트 효과 등
- [ ] **`outline: none` 금지** — 포커스 표시를 지우지 않는다

### 7.5 기능
- [ ] **다크모드 미구현** — v1 범위 밖. 종이색 배경이 이 디자인의 정체성이므로 다크모드는 별개 디자인을 요구한다
- [ ] **커스텀 커서 금지**
- [ ] **스크롤 하이재킹 금지**

---

## 8. 결정 대기 항목

| 항목 | 상태 | 비고 |
| --- | --- | --- |
| 인트로 한 줄 소개 문구 | 미정 | 15px 한 문장 |
| 로고 최종 서체 | 임시 (EB Garamond) | 대안: Cormorant Garamond, Libre Baskerville |
| 로고를 텍스트 조판 / 이미지 파일 | 미정 | 텍스트 권장 (반응형·용량 이점) |
| 헤더에 표기할 이름 | 미정 | |
| 푸터 연락 채널 | 미정 | 이메일 필수, GitHub 권장 |
| ~~모달 섹션 제목 언어~~ | **결정** | 영문 대문자 (§6.8) — 모노스페이스 서체 통일 때문 |

---

## 9. 구현 체크리스트

빌드 완료 시 확인할 항목:

- [ ] 팔레트 8개 값이 CSS 변수로 정의되어 있고, 하드코딩된 색상값이 없다
- [ ] 서체 3종만 로드된다
- [ ] 모노스페이스가 §3.1의 UI/라벨 요소에만 쓰였고, 읽는 텍스트(카드 설명, 모달 본문)에는 없다
- [ ] 모노스페이스 지정 요소에 `--font-body` fallback이 명시돼 있다
- [ ] 모노스페이스 굵기가 `400`이다 (예외: ENTER 버튼)
- [ ] 이탤릭이 어디에도 없다
- [ ] 그림자가 모달에만 있다
- [ ] 포인트 컬러가 링크·배지·포커스 링에만 쓰였다
- [ ] `coming soon` 카드 8개가 클릭되지 않는다
- [ ] 키보드 `Tab`으로 모든 요소에 도달할 수 있고 포커스 링이 보인다
- [ ] 인트로에서 `Enter` 키로 진입된다
- [ ] 모달이 `Esc`로 닫히고, 배경 스크롤이 잠긴다
- [ ] `prefers-reduced-motion` 설정 시 모션이 멈춘다
- [ ] 모바일 1열, 태블릿 2열, 데스크톱 4열로 정확히 나뉜다
