# MY HUB — PRD

> 개인 프로젝트 허브
> 작성일: 2026-08-13 / 상태: 초안

---

## 1. 개요

### 1.1 한 줄 정의
지금까지 만든(그리고 만들고 있는) 프로젝트를 한 곳에 모아 보여주는 개인 허브.

### 1.2 이름
`MY HUB`

프로젝트 성격과 무관한 중립적인 그릇으로 유지한다. 향후 책·문학 외의 프로젝트가 들어와도 이름을 재고하지 않는다.

### 1.3 배경
현재 진행 중인 프로젝트가 여러 개로 늘어나면서, 각 프로젝트가 흩어져 있고 전체를 한눈에 볼 수 있는 진입점이 없다. 또한 채용 과정에서 "무엇을 만들었는지" 보여줄 단일 URL이 필요하다.

---

## 2. 목적과 타겟

### 2.1 타겟

| 순위 | 대상 | 원하는 것 |
| --- | --- | --- |
| 1순위 | 채용 담당자 / 협업 제안자 | 이 사람이 무엇을 만들 수 있는지, 어떤 판단을 하는 사람인지 |
| 2순위 | 본인 | 만들었던 것들의 복기, 진행 상태 확인 |

### 2.2 타겟 충돌 해소 원칙
두 타겟이 원하는 정보(문제 정의 → 판단 → 결과)는 대부분 겹친다. 다만 **공개 화면의 톤과 문장은 채용 담당자 기준으로 작성**하고, 복기용 정보(배운 것, 진행 상태)를 그 안에 자연스럽게 얹는다. 개인 메모 톤으로 쓰지 않는다.

### 2.3 성공 기준
- 채용 담당자가 이 URL 하나만 받아도 "무엇을 만드는 사람인지" 파악할 수 있다.
- 새 프로젝트가 생겼을 때 데이터 한 건 추가로 카드가 채워진다. (코드 수정 없음)
- 본인이 프로젝트 상세를 다시 열었을 때, 당시의 판단 근거가 남아 있다.

---

## 3. 범위

### 3.1 In scope (v1)
- 인트로 페이지 (로고 + 한 줄 소개 + ENTER)
- 프로젝트 목록 페이지 (8개 카드 그리드)
- 프로젝트 상세 모달
- 상단 이름 표기 / 하단 연락처
- 8개 카드 전체 `coming soon` 상태 지원

### 3.2 Out of scope (v1)
- 별도 About 페이지 — 이름은 상단, 연락처는 footer로 분산
- 상세 전용 페이지 (`/projects/[slug]`) — 모달로 대체
- 어드민 UI — 데이터는 코드 내 파일로 관리
- 다국어

### 3.3 리스크 및 결정 사항
**리스크**: 8개 카드 전체가 `coming soon` 상태다. 채용 담당자 관점에서 클릭 가능한 결과물이 없는 8개의 빈 칸은 "계획이 많은 사람"보다 "완주한 게 없는 사람"으로 읽힐 수 있다.

**v1 결정**: 그대로 진행한다. 지금은 껍데기(레이아웃, 데이터 구조, 모달)를 완성하는 것이 목적이며, 내용은 이후 채운다.

**완화 조건**: 채용 담당자에게 링크를 공유하는 시점 전까지, 최소 1~2개 카드는 실제 내용으로 채운다. 이때 `coming soon` 슬롯은 "채워질 자리"로 읽히고, 시선은 실제 프로젝트에 집중된다.

---

## 4. 정보 구조

```
/                     인트로
└── /projects         프로젝트 목록 (8개 카드)
    └── 상세 모달      카드 클릭 시 오버레이
```

페이지는 2개. 상세는 페이지가 아니라 모달이므로 URL이 늘지 않는다.

---

## 5. 화면 요구사항

### 5.1 인트로 (`/`)

**목적**: 진입 의식. 바로 목록을 보여주지 않고 한 번 멈추게 해서 허브라는 공간감을 만든다.

**구성** (위 → 아래, 중앙 정렬)

| 요소 | 내용 | 비고 |
| --- | --- | --- |
| 로고 | `MY HUB` | 화면에서 가장 큰 요소. 텍스트 로고 또는 이미지 로고 |
| 한 줄 소개 | (문구 미정) | 로고 대비 작게. 무엇을 만드는 사람인지 한 문장 |
| ENTER 버튼 | `ENTER` | 클릭 시 `/projects` 이동 |

**요구사항**
- `ENTER` 클릭 외에 `Enter` 키 입력으로도 진입 가능
- 스크롤이 발생하지 않는 1화면 구성 (`100dvh`)
- 인트로에는 상단 이름 표기 / footer 연락처를 노출하지 않는다 (인트로는 로고에 집중)
- 페이지 진입 시 로고 → 소개 → 버튼 순서로 짧은 fade-in

**결정 필요**
- 한 줄 소개 문구
- 로고를 텍스트로 조판할지 이미지 파일로 넣을지

---

### 5.2 프로젝트 목록 (`/projects`)

**구성**

```
┌─────────────────────────────────────┐
│ 채슬                       (header) │
├─────────────────────────────────────┤
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐        │
│  │ 01 │ │ 02 │ │ 03 │ │ 04 │        │
│  └────┘ └────┘ └────┘ └────┘        │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐        │
│  │ 05 │ │ 06 │ │ 07 │ │ 08 │        │
│  └────┘ └────┘ └────┘ └────┘        │
├─────────────────────────────────────┤
│ 연락처                      (footer)│
└─────────────────────────────────────┘
```

**헤더**
- 이름 표기 (예: `채슬`)
- 클릭 시 인트로(`/`)로 복귀

**그리드**
- 카드 8개 고정
- 반응형: 데스크톱 4열 × 2행 / 태블릿 2열 × 4행 / 모바일 1열 × 8행

**카드 구성**

| 요소 | 필수 | 내용 |
| --- | --- | --- |
| 썸네일 | 선택 | 이미지 없으면 fallback (단색 배경 + 제목 텍스트) |
| 순번 | 필수 | `01` ~ `08` |
| 제목 | 필수 | `coming soon` 상태면 `Coming Soon` |
| 한 줄 설명 | 선택 | 없으면 영역을 비우고 높이는 유지 |
| 상태 배지 | 필수 | `coming soon` / `진행중` / `완료` |

**카드 상태별 동작**

| 상태 | 클릭 | 시각 처리 |
| --- | --- | --- |
| `coming soon` | 비활성 (모달 열리지 않음) | 낮은 명도, hover 시 커서 변화 없음 |
| `진행중` / `완료` | 상세 모달 오픈 | hover 시 상승 또는 테두리 강조 |

> 8개 전체가 `coming soon`인 v1에서는 클릭 가능한 카드가 없다. 이는 의도된 상태다.

**푸터**
- 연락처: 이메일 / GitHub / (선택) LinkedIn
- 이메일은 `mailto:` 링크, 외부 링크는 새 탭

---

### 5.3 프로젝트 상세 모달

**오픈 조건**: `coming soon`이 아닌 카드 클릭

**항목** (표시 순서)

| # | 항목 | 필수 | 설명 |
| --- | --- | --- | --- |
| 1 | 제목 | 필수 | 프로젝트 이름 |
| 2 | 이미지 | 선택 | 스크린샷 등. 없으면 영역 자체를 렌더하지 않는다 |
| 3 | 문제 정의 | 필수 | 왜 만들었는지, 어떤 불편을 봤는지 |
| 4 | 해결 방식 | 필수 | 어떻게 풀었는지, 핵심 기능 |
| 5 | 기술 스택 | 필수 | 무엇으로 만들었는지 (태그 형태) |
| 6 | 주요 판단 | 필수 | 갈림길에서 무엇을 선택하고 무엇을 버렸는지 |
| 7 | 결과 / 배운 것 | 필수 | 채용용 성과 + 복기용 회고 |
| 8 | 링크 | 선택 | 배포 URL, GitHub |

> 6번 **주요 판단**이 이 허브의 핵심 항목이다. 기능 목록은 누구나 쓸 수 있지만, 무엇을 버렸는지는 실제로 만든 사람만 쓸 수 있다. 채용 담당자가 실력을 판단하는 근거가 여기에 있다.

**동작 요구사항**
- 닫기: 우상단 X / 오버레이 클릭 / `Esc`
- 모달 오픈 중 배경 스크롤 잠금
- 내용이 길면 모달 내부 스크롤
- 오픈 시 포커스를 모달로 이동, 닫을 때 원래 카드로 복귀
- 이미지 없는 프로젝트에서 상단 여백이 어색해지지 않도록, 이미지 부재 시 제목이 곧바로 최상단에 온다

---

## 6. 데이터 모델

Supabase를 쓰지 않는다. 프로젝트 8건은 정적 데이터이며 변경 빈도가 낮으므로, 코드 내 파일로 관리하고 배포로 반영한다. DB를 붙이면 관리 비용만 늘고 얻는 것이 없다.

```ts
// data/projects.ts

type ProjectStatus = 'coming-soon' | 'in-progress' | 'done';

interface Project {
  order: number;              // 1-8, 카드 순번
  status: ProjectStatus;
  title: string | null;       // coming-soon이면 null 허용
  tagline?: string;           // 카드의 한 줄 설명
  thumbnail?: string;         // 카드 썸네일 경로 (선택)
  detail?: {
    image?: string;           // 상세 이미지 (선택)
    problem: string;          // 문제 정의
    solution: string;         // 해결 방식
    stack: string[];          // 기술 스택
    decisions: string;        // 주요 판단
    outcome: string;          // 결과 / 배운 것
    links?: {
      live?: string;
      github?: string;
    };
  };
}
```

**규칙**
- `status: 'coming-soon'`이면 `detail`은 없어도 된다.
- `status`가 `coming-soon`이 아니면 `detail`은 필수다.
- 배열 길이는 항상 8. 새 프로젝트는 `coming-soon` 슬롯을 덮어쓰는 방식으로 추가한다.

---

## 7. 기술 스택

| 항목 | 선택 | 근거 |
| --- | --- | --- |
| 프레임워크 | Next.js (App Router) | 기존 프로젝트와 동일한 스택, 학습 비용 없음 |
| 스타일 | Tailwind CSS | 빠른 조판 |
| 데이터 | 코드 내 정적 파일 | 8건 정적 데이터에 DB는 과잉 |
| 배포 | Vercel | 기존과 동일 |
| 도메인 | 미정 | |

---

## 8. 결정 대기 항목

| 항목 | 상태 |
| --- | --- |
| 인트로 한 줄 소개 문구 | 미정 |
| 로고 형태 (텍스트 조판 / 이미지) | 미정 |
| 8개 프로젝트의 제목·설명 | 미정 (전체 coming soon) |
| 푸터에 넣을 연락 채널 확정 | 미정 |
| 도메인 | 미정 |
| 전체 톤 (다크 / 라이트) | 미정 |

---

## 9. 향후 (v2+)

- 실제 프로젝트로 카드 채우기 (완독, 문학 인물 대화 우선)
- 카드가 8개를 넘어갈 때의 그리드 확장 또는 페이지네이션
- 상세를 공유 가능한 별도 URL로 승격 (`/projects/[slug]`) — 특정 프로젝트만 링크로 보낼 필요가 생기면
- 프로젝트별 태그 필터
